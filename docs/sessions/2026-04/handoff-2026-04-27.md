# Handoff — 2026-04-27

## O que foi feito nesta sessão

Fase de planejamento BMAD **100% concluída**. Todas as 69 stories criadas, validadas e com issues críticos corrigidos.

## Artefatos produzidos

| Artefato | Localização |
|---|---|
| PRD | `_bmad-output/planning-artifacts/prd.md` |
| Arquitetura (CA) | `_bmad-output/planning-artifacts/architecture.md` |
| Épicos e Stories (CE) | `_bmad-output/planning-artifacts/epics.md` |
| 69 Story files | `_bmad-output/implementation-artifacts/*.md` |
| Sprint status | `_bmad-output/implementation-artifacts/sprint-status.yaml` |
| Relatório PDF | `_bmad-output/edital-em-questoes-backlog-bmad.pdf` |

## Status das stories

Todos os 11 épicos `in-progress`. Todas as 69 stories `ready-for-dev`.

## 4 Correções aplicadas (issues críticos resolvidos)

- **C1** `3-1`: `question_status_enum` agora inclui `'pending_review'` (necessário para Epic 8)
- **C2** `1-3`: signup passa `data: { full_name: input.name }` (alinha com trigger `handle_new_user`)
- **C3** `3-3`: `selected_answer` aceita A-F (alinha com questões de até 6 alternativas)
- **C4** `5-5`: MV de histórico usa `time_spent_ms / 1000.0` e `answered_at` (colunas reais)

## Próximo passo

1. **Criar `CLAUDE.md` no projeto** com contexto para agentes AIOS
2. **Iniciar implementação com `@dev`** pela Story 1.1 (Setup Técnico Inicial)
3. Story 1.1 requer ações manuais do André: criar projeto Supabase + Vercel + configurar secrets

## Dependências críticas a respeitar

- ADR 4.1 deve estar aprovado antes de implementar Story 4.3
- ADR 5.1 deve estar aprovado antes de implementar Stories 5.2+
- Sequência dentro do MVP: 1 → 2 → 3 → 4 → 5 → 6

## Decisões pendentes (não bloqueantes para Story 1.1)

- Fonte de verdade para plano do usuário (`users.plan` vs `app_metadata.plan`)
- Shape canônico de alternativas (confirmar em Story 3.1 antes de 3.3)
- Renumeração de ADRs (conflito 0003 entre Story 2.5 e 7.1)
